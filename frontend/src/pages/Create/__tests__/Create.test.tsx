import React from "react";

import { Create } from "..";

describe("Create", () => {
  describe("Layout", () => {
    it("should render title textfield, desc textfield, template select, artwork args input on the left side", () => {});

    it("should render artwork frame on the right side", () => {});

    it("should render randomize button, save & mint button on the right bottom", () => {});

    it("should render content textfield only when debug mode is on", () => {});
  });
});
